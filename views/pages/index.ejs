<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../partials/head'); %>
</head>
<body class="container">

<header>
    <%- include('../partials/header'); %>
</header>

<main>
    <div class="jumbotron">
        <p>Main Content will go here</p>
        <form >
            <div class='form-group'>
                <label>Roaster</label>
                <input type='text' class='form-control' placeholder="coffee roaster">   
            </div>
            <div class='form-row'>
                <div class = 'col'>
                    <label>Region</label>
                    <input type='text' class='form-control' placeholder="region">
                </div>
                <div class ='col'>
                    <label>Tasting Notes</label>
                    <select multiple class ='form-control'>
                        <% for (let i = 0; i < taste_profile.length; i++) { %>
                        <option><%= taste_profile[i]%></option>
                        <% } %>
                    </select>
                </div>
            </div>
            <div class='form-group'>
                <label>Brew Method</label>
                <select class='form-control'>
                    <% for (let i = 0; i < brewmethods.length; i++ ) { %>
                    <option> <%= brewmethods[i]%></option>
                    <% } %>
                </select>
            </div>
            <div class='form-group'>
                <label>Notes</label>
                <textarea class ='form-control' rows="3"></textarea>
            </div>

            <button type="submit" method = 'POST' class="btn btn-primary">Submit</button>
        </form>

        
    </div>


</main>

</body>
</html>
