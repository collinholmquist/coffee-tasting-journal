<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../partials/head'); %>
</head>
<body class="container">

<header>
    <%- include('../partials/header'); %>
</header>

<main>
    <div class="jumbotron">
        
        <div class = 'row'>
            <div class = 'col-auto me-auto'>
               <h3 >Editing...</h3> 
            </div>
            
            <div class = 'col-auto'>
                <a href='/user/'><button class='btn btn-secondary'>Cancel</button></a>
            </div>

            <div class ='col-auto'>
                <a class='confirm-delete' 
                            data-confirm ='Are you sure you want to delete this?'>
                            <form action='/user/remove/<%=newRes.entry_id%>' method='POST' id='delete-form'>
                                <button 
                                type='submit' class = 'btn btn-danger'>
                                Delete Entry
                            </button>
                            </form>   
            </a> 
            </div>
            
        </div>

        <form action ="/user/<%=newRes.entry_id%>" method="POST">
            <div class='form-group'>
                <label>Roaster</label>
                <input type='text' name = 'roaster_name' class='form-control' value="<%= newRes.roaster_name %>">   
            </div>
            <div class='form-row'>
                <div class = 'col'>
                    <label>Region</label>
                    <input type='text' name = 'region_name'  class='form-control' value="<%= newRes.region_name %>">
                </div>
                <div class ='col'>
                    <label>Tasting Notes</label>
                    <select name = 'tasting_notes' multiple class ='form-control'>
                        <% for (let i = 0; i < taste_profile.length; i++) { %>
                        <option><%= taste_profile[i]%></option>
                        <% } %>
                    </select>
                </div>
            </div>
            <div class='form-group'>
                <label>Brew Method</label>
                <select name = 'brew_method' class='form-control'>
                    <% for (let i = 0; i < brewmethods.length; i++ ) { %>
                    <option> <%= brewmethods[i]%></option>
                    <% } %>
                </select>
            </div>
            <div class='form-group'>
                <label>Notes</label>
                <textarea name = 'comments' class ='form-control' rows="3" > <%= newRes.comments %></textarea>
            </div>

            <button type="submit" class="btn btn-primary">Save</button>     
        </form>  
    </div>


</main>

<%- include('../partials/script'); %>

</body>
</html>